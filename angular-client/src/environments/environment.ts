export const environment = {
  production: false,
  serverUrl: "http://localhost:3001",  // For REST endpoints (register, login, preferences)
  socketUrl: "http://localhost:3000"   // For WebSocket connections (todos)
};
